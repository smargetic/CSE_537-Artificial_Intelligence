To run either search algorithm, run userInput.py . From there, the console will prompt you as
to which search method you would like to preform, and if it is A* Search, it will prompt you
with corresponding heuristics.